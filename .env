# Explorer Environment Variables

# Domain and URLs
EXTERNAL_DOMAIN=dabendan.ddns.net
FRONTEND_URL=https://dabendan.ddns.net/explorer/

# Deployment paths
EXPLORER_BUILD_DIR=./build/
EXPLORER_DEPLOY_TARGET=../openclaw-web/public/explorer/

# Frontend Configuration
# React app's default port during build is not critical, 
# but we need to ensure BACKEND_PORT in deploy.sh matches here
PORT=5000
REACT_APP_API_URL=http://localhost:5000/api
